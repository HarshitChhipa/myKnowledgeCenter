# As I am Right now onto Laravel this is my point to note my learnings.
 ## Make a controller
 
 - `php artisan make::controller ControllerName`
 
 ## Create Model
 - `php artisan make::model modelName -m` {Add -m to add the migration}
